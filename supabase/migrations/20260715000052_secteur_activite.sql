-- =============================================================================
-- Nova Comptabilité — 0052 : (désactivée) Secteur d'activité
-- =============================================================================
-- Fonctionnalité retirée : le secteur d'activité n'est plus utilisé (les cas
-- d'imputation spécifiques sont traités via les « Règles » de codification).
-- Migration neutralisée (no-op) pour ne pas créer de colonne inutile.
-- =============================================================================

select 1;
